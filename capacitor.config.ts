import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'contacts-management',
  webDir: 'dist/ContactsManagement',
  bundledWebRuntime: false
};

export default config;
